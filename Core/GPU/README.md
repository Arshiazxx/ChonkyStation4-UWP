# GPU boundary

The GPU subsystem will define the PS4-facing command and resource contracts.
The existing Xbox D3D12 host diagnostic remains a frontend/backend service;
it is not pulled into Core by the ELF foundation.
